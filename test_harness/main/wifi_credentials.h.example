#pragma once
/* Copy this file to wifi_credentials.h and fill in your network details.
 * wifi_credentials.h is gitignored — never commit it. */

#define WIFI_SSID  "your_network_name"
#define WIFI_PASS  "your_password"
